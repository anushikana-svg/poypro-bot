app = "poypro-bot"
primary_region = "ams"

[build]

[env]
  FLY_PROCESS_GROUP = "app"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1

[checks]
  [checks.alive]
    type = "tcp"
    interval = "15s"
    timeout = "2s"
    grace_period = "5s"
